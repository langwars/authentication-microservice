Notes for Go:

1. The initial code that was produced by ChatGPT o1 was extremely slow (200 req/s).
2. With 2 rounds of optimizations by Claude it reached an average of ~27k req/s (still pretty slow).
3. Go is not recommended for use with AI.